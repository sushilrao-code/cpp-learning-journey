#include <iostream>
using namespace std;
int main()
{
   const  float p = 3.14, 
   float r;
    cout << "Enter your R = ";
    cin >> r;
    // float A = (p * (r * r));
    // cout << " AREA_OF_CIRCLE= " << A;
    cout <<"Area = "<<pi*r*r;
    return 0;
}
